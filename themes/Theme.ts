/**
 * Theme colors specifications.
 */
export interface Theme {
  primary: string;
  background: string;
  foreground: string;
  selected: string;
  disabled: string;
  yellow: string;
  blue: string;
  green: string;
}
