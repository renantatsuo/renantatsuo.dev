/**
 * Theme colors specifications.
 */
export interface Theme {
  primary: string;
  background: string;
  backgroundDarker: string;
  foreground: string;
  foregroundDarker: string;
  selected: string;
  disabled: string;
  yellow: string;
  blue: string;
  green: string;
}
